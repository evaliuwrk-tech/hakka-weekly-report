# 哈客組 週報填報系統 — 設定說明

一套「線上填報 → 下載統一格式 Word → 成員自己上傳雲端 → 組長看誰交了」的半自動系統。
第一次設定約 15 分鐘，之後每週零維護。

## 檔案結構

| 檔案 | 用途 | 誰用 |
|------|------|------|
| `index.html` | 填報頁（選姓名、日曆選週次、產生 Word、登記） | **組員**（分享這條連結） |
| `dashboard.html` | 進度看板（✓／逾期標紅／催繳清單） | **組長專用**（勿分享給組員） |
| `config.js` | 名單、網址等設定（兩頁共用，日後只改這裡） | 組長維護 |
| `Code.gs` | Google Apps Script 後端 | 貼到試算表 |

- **組員連結**：`https://evaliuwrk-tech.github.io/hakka-weekly-report/`
- **組長看板**：`https://evaliuwrk-tech.github.io/hakka-weekly-report/dashboard.html`

---

## 一、系統怎麼運作（一張圖看懂）

```
成員開網頁填報 ──▶ 按「產生週報」
                    ├─▶ 下載 哈客組_週報_2026-W34_王小明.docx  ──▶ 成員自己拖到雲端資料夾
                    └─▶ 偷偷在你的 Google 試算表登記一筆「已繳交」
                                        │
組長開同一個網頁的「📊 進度看板」分頁 ◀──┘  一眼看到本週誰交了、誰還沒交
```

- **週報內文**只存在你的**私人 Google 試算表**，看板頁只顯示 ✓／✗，不會外洩內容。
- Word 檔完全在瀏覽器產生，**不依賴任何外部服務**，離線也能用。

---

## 二、設定步驟

### 步驟 1：建立 Google 試算表 + 貼上後端程式

1. 到 [sheets.new](https://sheets.new) 建立一張新的 Google 試算表，命名為「哈客組週報登記」。
2. 上方選單 **擴充功能 → Apps Script**。
3. 把預設的空白內容全部刪掉，改貼上 `Code.gs` 的**全部內容**。
4. 按 💾 儲存（Ctrl+S）。

### 步驟 2：部署為網頁應用程式

1. 在 Apps Script 編輯器右上角，按 **部署 → 新增部署作業**。
2. 齒輪圖示 → 選擇類型 **網頁應用程式（Web app）**。
3. 設定：
   - **執行身分（Execute as）**：`我（你的帳號）`
   - **具有存取權的使用者（Who has access）**：`任何人（Anyone）`
     > ⚠️ 一定要選「任何人」，否則成員填報時無法登記、看板也讀不到。這只允許寫入/讀取這一張表的狀態，不會外流你的其他檔案。
4. 按 **部署**，第一次會要求授權 → 按「授權存取權」→ 選你的帳號 →（若出現「Google 尚未驗證這個應用程式」）點「進階 → 前往…（不安全）」→ 允許。這是你自己寫的程式，安全。
5. 複製最後出現的 **網頁應用程式網址**，長得像：
   `https://script.google.com/macros/s/AKfycb.../exec`

### 步驟 3：填入設定

打開 **`config.js`**（設定都集中在這一個檔），改這幾行：

```js
window.HAKKA_CONFIG = {
  teamName: "哈客組",
  members: ["劉怡君（組長）", "涂政強", "黃敏琪", "劉美君"],   // ← 你和三位組員的姓名
  appsScriptUrl: "https://script.google.com/macros/s/AKfycb.../exec",  // ← 步驟 2 的網址
  weeksToShow: 8,
  formUrl: "https://evaliuwrk-tech.github.io/hakka-weekly-report/",   // 填報頁網址（催繳訊息用）
  deadlineWeekday: 5,   // 截止＝週五（1=一…7=日）
  deadlineHour: 18      // 截止＝18:00 下班；過了就算「逾期」標紅
};
```

存檔。**完成！**

### 步驟 4：讓成員能用

把整個資料夾上傳到 GitHub Pages（已完成）。分享時：

- 給組員 → **填報頁**連結（index）
- 你自己看 → **dashboard.html** 那條（有逾期標紅、催繳清單）

---

## 三、每週流程

| 對象 | 做什麼 |
|------|--------|
| **組員** | 週五下班前開網頁 → 選姓名 → 填三欄 → 按「產生週報」→ 下載的 `.docx` 上傳到自己組別的雲端資料夾 |
| **組長（你）** | 開網頁「📊 進度看板」→ 看本週 ✓／✗ → 對還沒交的人 catch 一下；要看內文就開 Google 試算表 |

補交上週：填報時用週次左邊的 **◀** 箭頭切到上一週即可。

---

## 三之二、LINE 自動催繳（選用）

> 每週五 **16:30**（截止 16:00 後半小時），系統自動把「本週逾期未交」名單推播到你們的 LINE 群組。
> ⚠️ 舊的 LINE Notify 已於 2025/3 停用，改用 **LINE Messaging API（官方帳號機器人）**。以下是一次性設定。

### A. 建立官方帳號 + 拿 Token
1. 到 **LINE Developers Console**（網址：`https://developers.line.biz/console/`）用你的 LINE 帳號登入。
2. 建立一個 **Provider**（隨便命名，如「哈客組」）→ 在其下建立 **Messaging API channel**（頻道名稱如「哈客組週報小幫手」）。
3. 進入該 channel → **Messaging API** 分頁 → 最下方 **Channel access token（long-lived）** 按 **Issue** → 複製那一長串。

### B. 把 Token 放進後端
1. Apps Script 編輯器左側齒輪 **專案設定（Project Settings）** → 下方 **指令碼屬性（Script Properties）** → **新增屬性**：
   - 屬性：`LINE_TOKEN`　值：貼上剛剛的 Channel access token → 儲存。

### C. 讓機器人進群、抓群組 ID
1. 回 LINE channel → **Messaging API** 分頁：
   - **Webhook URL** 填你的 Apps Script `.../exec` 網址 → **Update** → 打開 **Use webhook**。
   - 往下 **LINE Official Account features** 兩項到 LINE Official Account Manager 設定：**Allow bot to join group chats** 開啟；自動回覆訊息可關掉。
2. 用手機把這個官方帳號**加入你們的週報 LINE 群組**（在群組 → 邀請 → 搜尋官方帳號）。
3. 在群組裡**隨便發一句話**（讓機器人收到事件）。
4. 回 Apps Script，上方函式選 **`showGroupId`** → 按 ▶ 執行 → 看「執行紀錄」若出現 `LINE_GROUP_ID = C....` 就代表抓到了。

### D. 開啟每週自動催繳
1. 上方函式選 **`installReminderTrigger`** → 按 ▶ 執行一次（會要求授權，允許）。這會建立「每週五約 16:30」的自動觸發。
2. （可選）想馬上測試：選 **`testLineHello`** 執行 → 群組應收到一則測試訊息；或選 **`testReminderNow`** 依實際逾期名單發一次。

完成後就全自動了：週五 16:00 截止、16:30 自動在群組點名未交的人。
> 日後若在填報頁改了成員名單，記得也把 `Code.gs` 最上方的 `MEMBERS` 改成一樣，再重新部署一次。

---

## 四、常見問題

**Q：成員重複按「產生週報」會不會重複登記？**
不會。系統以「週次＋姓名」判斷，同一人同一週再送出會**覆蓋**舊紀錄，試算表保持乾淨。

**Q：看板顯示「連線逾時 / 讀取失敗」？**
99% 是步驟 2 的存取權沒選「任何人」，或網址貼錯。回 Apps Script 重新部署一次、確認網址結尾是 `/exec`。

**Q：改了 Code.gs 之後？**
要重新「部署 → 管理部署作業 → 編輯（鉛筆）→ 版本選『新版本』→ 部署」，網址不變。

**Q：想連週報內文也顯示在看板上？**
不建議（GitHub Pages 是公開的，會讓內文外洩）。內文請一律從你的**私人試算表**看。

**Q：Word 檔打得開嗎？格式跑掉？**
檔案是標準 `.docx`，Word、Google 文件、WPS 都能開。標題、三段落格式固定，達成「統一格式」。
