# 哈客組 週報填報系統 — 設定說明

一套「線上填報 → 下載統一格式 Word → 成員自己上傳雲端 → 組長看誰交了」的半自動系統。
只有兩支檔案要處理：`index.html`（網頁）和 `Code.gs`（後端）。第一次設定約 15 分鐘，之後每週零維護。

---

## 一、系統怎麼運作（一張圖看懂）

```
成員開網頁填報 ──▶ 按「產生週報」
                    ├─▶ 下載 哈客組_週報_2026-W34_王小明.docx  ──▶ 成員自己拖到雲端資料夾
                    └─▶ 偷偷在你的 Google 試算表登記一筆「已繳交」
                                        │
組長開同一個網頁的「📊 進度看板」分頁 ◀──┘  一眼看到本週誰交了、誰還沒交
```

- **週報內文**只存在你的**私人 Google 試算表**，看板頁只顯示 ✓／✗，不會外洩內容。
- Word 檔完全在瀏覽器產生，**不依賴任何外部服務**，離線也能用。

---

## 二、設定步驟

### 步驟 1：建立 Google 試算表 + 貼上後端程式

1. 到 [sheets.new](https://sheets.new) 建立一張新的 Google 試算表，命名為「哈客組週報登記」。
2. 上方選單 **擴充功能 → Apps Script**。
3. 把預設的空白內容全部刪掉，改貼上 `Code.gs` 的**全部內容**。
4. 按 💾 儲存（Ctrl+S）。

### 步驟 2：部署為網頁應用程式

1. 在 Apps Script 編輯器右上角，按 **部署 → 新增部署作業**。
2. 齒輪圖示 → 選擇類型 **網頁應用程式（Web app）**。
3. 設定：
   - **執行身分（Execute as）**：`我（你的帳號）`
   - **具有存取權的使用者（Who has access）**：`任何人（Anyone）`
     > ⚠️ 一定要選「任何人」，否則成員填報時無法登記、看板也讀不到。這只允許寫入/讀取這一張表的狀態，不會外流你的其他檔案。
4. 按 **部署**，第一次會要求授權 → 按「授權存取權」→ 選你的帳號 →（若出現「Google 尚未驗證這個應用程式」）點「進階 → 前往…（不安全）」→ 允許。這是你自己寫的程式，安全。
5. 複製最後出現的 **網頁應用程式網址**，長得像：
   `https://script.google.com/macros/s/AKfycb.../exec`

### 步驟 3：填入設定

打開 `index.html`，找到最上方標了 **★★★ 設定區 ★★★** 的那一段，只改這四行：

```js
const CONFIG = {
  teamName: "哈客組",
  members: ["組長（我）", "成員一", "成員二", "成員三"],   // ← 改成你和三位組員的真實姓名
  appsScriptUrl: "https://script.google.com/macros/s/AKfycb.../exec",  // ← 貼上步驟 2 的網址
  weeksToShow: 8
};
```

存檔。**完成！**

### 步驟 4：讓成員能用（擇一）

- **最簡單**：把 `index.html` 上傳到你現有的 GitHub Pages（跟你之前放抽籤工具、儀表板一樣），拿到網址分享給三位組員。
- 或用任何靜態空間 / 直接把 `index.html` 傳給成員，用瀏覽器開也能填、能下載（只是需要有網路才能登記繳交狀態）。

---

## 三、每週流程

| 對象 | 做什麼 |
|------|--------|
| **組員** | 週五下班前開網頁 → 選姓名 → 填三欄 → 按「產生週報」→ 下載的 `.docx` 上傳到自己組別的雲端資料夾 |
| **組長（你）** | 開網頁「📊 進度看板」→ 看本週 ✓／✗ → 對還沒交的人 catch 一下；要看內文就開 Google 試算表 |

補交上週：填報時用週次左邊的 **◀** 箭頭切到上一週即可。

---

## 四、常見問題

**Q：成員重複按「產生週報」會不會重複登記？**
不會。系統以「週次＋姓名」判斷，同一人同一週再送出會**覆蓋**舊紀錄，試算表保持乾淨。

**Q：看板顯示「連線逾時 / 讀取失敗」？**
99% 是步驟 2 的存取權沒選「任何人」，或網址貼錯。回 Apps Script 重新部署一次、確認網址結尾是 `/exec`。

**Q：改了 Code.gs 之後？**
要重新「部署 → 管理部署作業 → 編輯（鉛筆）→ 版本選『新版本』→ 部署」，網址不變。

**Q：想連週報內文也顯示在看板上？**
不建議（GitHub Pages 是公開的，會讓內文外洩）。內文請一律從你的**私人試算表**看。

**Q：Word 檔打得開嗎？格式跑掉？**
檔案是標準 `.docx`，Word、Google 文件、WPS 都能開。標題、三段落格式固定，達成「統一格式」。
